@using ProductionTracker.Data
@using ProductionTracker.Web.Models
@model ColorVM

@{
    ViewBag.Title = "Colors";
}


<div class="row " style="margin-top:25px;">
    
        <div class="col-md-5 col-lg-offset-1 well" style="background-color:burlywood; overflow:scroll; max-height:750px;">
            <form class="form" action="/Item/AddColors" method="post">
                <h3 id="color-form-header" style="text-align:center; font-weight:bold;">Enter the colors you want to add </h3>
                <br />
                <button id="btn-row-adder" style="text-align:end;" type="button" class="btn btn-success">Add more rows</button>
                <table class="table table-bordered table-responsive" id="color-form-table">
                    <tr>
                        <th style="text-align:center;">Id</th>
                        <th style="text-align:center;">Color</th>
                    </tr>
                    <tr>
                        <td>
                            <input type="text" placeholder="Id" name="colors[0].Id" class="form-control" />
                        </td>
                        <td>
                            <input type="text" placeholder="Color" name="colors[0].Name" class="form-control" />
                        </td>
                    </tr>
                </table>
                <button class="btn btn-primary btn-lg">Submit</button>
            </form>
</div>
    <div class="col-md-5 col-lg-offset-1" style="overflow:scroll; background-color:cadetblue;">
        <h3 style="text-align:center; font-weight:bold;">Colors </h3>
        <table class="table table-bordered table-responsive table-striped table-hover">
            <tr>
                <th style="text-align:center;">Id</th>
                <th style="text-align:center;">Color</th>
            </tr>
            @foreach (var color in Model.Colors)
            {
            <tr>
                <td>@color.Id</td>
                <td>@color.Name</td>
            </tr>
            }
        </table>
        </div>

    </div>

@section scripts{
    <script src="~/Scripts/SiteScripts/Colors.js"></script>
    }

